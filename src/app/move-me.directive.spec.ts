import { MoveMeDirective } from './move-me.directive';

describe('MoveMeDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveMeDirective();
    expect(directive).toBeTruthy();
  });
});
